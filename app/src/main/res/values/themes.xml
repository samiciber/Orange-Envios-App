<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Logistiq" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Logistiq" parent="Base.Theme.Logistiq" />
    <style name="PinPadButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_rowWeight">1</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="backgroundTint">@android:color/white</item>
        <item name="cornerRadius">24dp</item>
    </style>

    <style name="PinPadButtonIcon" parent="PinPadButton">
        <item name="android:padding">16dp</item>
        <item name="android:tint">@android:color/black</item>
        <item name="android:text">""</item>
    </style>

    <color name="light_blue_bg">#E0F7FA</color>
    <color name="red">#FF0000</color>
</resources>